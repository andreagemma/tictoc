name: Build and publish
run-name: Build and publish - ${{ inputs.commit_name || github.ref_name }}

on:
  workflow_call:
    inputs:
      commit_name:
        description: Commit name shown in run title
        required: false
        type: string
      publish:
        description: Publish the built distributions to PyPI
        required: true
        default: false
        type: boolean
  workflow_dispatch:
    inputs:
      commit_name:
        description: Commit name shown in run title
        required: false
        type: string
      publish:
        description: Publish the built distributions to PyPI
        required: true
        default: false
        type: boolean

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
      - run: python -m pip install --upgrade build twine
      - run: python -m build
      - run: python -m twine check dist/*
      - name: Smoke test built wheel
        run: |
          python -m venv wheel-smoke-test
          wheel-smoke-test/bin/python -m pip install dist/*.whl
          wheel-smoke-test/bin/python -c "import tictoc; print(tictoc.__version__)"
      - uses: actions/upload-artifact@v7
        with:
          name: python-distributions
          path: dist/
          if-no-files-found: error

  publish:
    if: ${{ inputs.publish }}
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/p/ga-tictoc
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@v8
        with:
          name: python-distributions
          path: dist/
      - uses: pypa/gh-action-pypi-publish@release/v1
